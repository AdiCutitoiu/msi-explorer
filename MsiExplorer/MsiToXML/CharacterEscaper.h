#pragma once

namespace SeqEscape
{
/**
 * @brief escapes a string in XML format
 *
 * @param aString the string to be escaped
 *
 * @returns the escaped string
 */
wstring Escape(const wstring & aString);
}
